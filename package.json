{
  "name": "master-paging-in-os",
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "install-all": "npm install --workspaces",
    "start:client": "npm --workspace client run dev",
    "start:server": "npm --workspace server run start"
  }
}
